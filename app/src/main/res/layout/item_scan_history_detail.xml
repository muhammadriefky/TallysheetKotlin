<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cardView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:strokeWidth="2dp"
    app:strokeColor="@color/divider">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- SKU Code -->
        <TextView
            android:id="@+id/tvSku"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/tvStatus"
            android:layout_marginEnd="8dp"
            tools:text="SKU-125" />

        <!-- Status Badge -->
        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textStyle="bold"
            android:padding="6dp"
            android:background="@drawable/progress_rounded"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="✅ Sesuai" />

        <!-- Target -->
        <ImageView
            android:id="@+id/iconTarget"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:src="@android:drawable/ic_menu_compass"
            android:tint="@color/info"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/tvSku"
            app:layout_constraintStart_toStartOf="parent" />

        <TextView
            android:id="@+id/tvTarget"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="@color/text_secondary"
            android:layout_marginStart="8dp"
            app:layout_constraintTop_toTopOf="@id/iconTarget"
            app:layout_constraintBottom_toBottomOf="@id/iconTarget"
            app:layout_constraintStart_toEndOf="@id/iconTarget"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="Target: 50 KARTON" />

        <!-- Scanned -->
        <ImageView
            android:id="@+id/iconScanned"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:src="@android:drawable/ic_menu_view"
            android:tint="@color/success"
            android:layout_marginTop="6dp"
            app:layout_constraintTop_toBottomOf="@id/iconTarget"
            app:layout_constraintStart_toStartOf="parent" />

        <TextView
            android:id="@+id/tvScanned"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="@color/text_primary"
            android:layout_marginStart="8dp"
            app:layout_constraintTop_toTopOf="@id/iconScanned"
            app:layout_constraintBottom_toBottomOf="@id/iconScanned"
            app:layout_constraintStart_toEndOf="@id/iconScanned"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="Scan: 45 Karton + 5 PCS" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
