<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Light Mode -->
    <style name="Base.Theme.HandheldApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorSurfaceVariant">@color/surface_variant</item>
        <item name="android:windowBackground">@color/background</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:textColorHint">@color/text_hint</item>

        <!-- Card colors -->
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary</item>

        <!-- Status bar and navigation -->
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/background</item>

        <!-- Navigation Drawer -->
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
    </style>

    <style name="Theme.HandheldApp" parent="Base.Theme.HandheldApp" />

    <!-- Drawer Arrow Style -->
    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/white</item>
    </style>

    <!-- Toolbar Style -->
    <style name="Widget.App.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/primary</item>
        <item name="titleTextColor">@color/white</item>
        <item name="subtitleTextColor">@color/toolbar_subtitle</item>
    </style>

    <!-- Navigation View Style for Light Mode -->
    <style name="Widget.App.NavigationView" parent="Widget.Material3.NavigationView">
        <item name="android:background">@color/nav_background</item>
        <item name="itemTextColor">@color/nav_item_text_color</item>
        <item name="itemIconTint">@color/nav_item_icon_tint</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.HandheldApp.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/primary</item>
        <item name="android:windowBackground">@drawable/bg_splash_gradient</item>
    </style>

    <!-- Navigation Item Shape -->
    <style name="ShapeAppearance.App.NavItem" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <!-- Tab Text Appearance -->
    <style name="TabTextAppearance" parent="TextAppearance.Material3.TitleSmall">
        <item name="android:textSize">13sp</item>
        <item name="android:textStyle">bold</item>
        <item name="textAllCaps">false</item>
    </style>

    <!-- ==================== FONT SIZE STYLES ==================== -->

    <!-- Small Font Size -->
    <style name="Theme.HandheldApp.FontSmall" parent="Theme.HandheldApp">
        <item name="android:textSize">12sp</item>
    </style>

    <!-- Normal Font Size (Default) -->
    <style name="Theme.HandheldApp.FontNormal" parent="Theme.HandheldApp">
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Large Font Size -->
    <style name="Theme.HandheldApp.FontLarge" parent="Theme.HandheldApp">
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Extra Large Font Size -->
    <style name="Theme.HandheldApp.FontXLarge" parent="Theme.HandheldApp">
        <item name="android:textSize">18sp</item>
    </style>
</resources>