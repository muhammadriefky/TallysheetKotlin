<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Main Menu -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_home"
            android:title="Dashboard" />
    </group>

    <!-- Tally Sheet -->
    <item android:title="Tally Sheet" app:showAsAction="collapseActionView" android:alphabeticModifiers="ALT">
        <menu>
            <item
                android:id="@+id/nav_tallysheet_aktif"
                android:icon="@drawable/ic_scan"
                android:title="Tallysheet Aktif" />
            <item
                android:id="@+id/nav_tallysheet_history"
                android:icon="@drawable/ic_history"
                android:title="History Tallysheet" />
        </menu>
    </item>

    <!-- Warehouse Operations -->
    <item android:title="Operasional Gudang">
        <menu>
            <item
                android:id="@+id/nav_putaway"
                android:icon="@drawable/ic_rack"
                android:title="Put Away / Rak" />
            <item
                android:id="@+id/nav_stock_opname"
                android:icon="@drawable/ic_inventory"
                android:title="Stock Opname" />
            <item
                android:id="@+id/nav_mutasi"
                android:icon="@drawable/ic_transfer"
                android:title="Mutasi Barang" />
            <item
                android:id="@+id/nav_btb"
                android:icon="@drawable/ic_receipt"
                android:title="BTB (Rusak/Reject)" />
        </menu>
    </item>

    <!-- Reports & History -->
    <item android:title="Laporan">
        <menu>
            <item
                android:id="@+id/nav_history"
                android:icon="@drawable/ic_history"
                android:title="History Scan" />
            <item
                android:id="@+id/nav_reports"
                android:icon="@drawable/ic_report"
                android:title="Laporan Gudang" />
        </menu>
    </item>

    <!-- Settings & Account -->
    <item android:title="Akun Pengaturan">
        <menu>
            <item
                android:id="@+id/nav_profile"
                android:icon="@drawable/ic_profile"
                android:title="Profil Saya" />
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_settings"
                android:title="Pengaturan" />
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_logout"
                android:title="Keluar" />
        </menu>
    </item>
</menu>